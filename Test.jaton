class Main {
    public static void main() {
        double a, b;
        print("Введите первое слагаемое: ");
        scan(a);
        print("Введите второе слагаемое: ");
        scan(b);
        println(a + " - " + b + " = " + (a - b));
    }
}
